# Git Exercise :

Repository Creation:

    Create a new Github Repository and clone this repository into my local system.

Develop branch Create:

    craete a develop branch.

Commit message Hook:

    After add commit-msg hook is added to ensure commit message.
    This hook is checked at least 10 word commit require.
    if commit message length is less than 10 words, the commit is rejected.

feature1 Branch Creation:

    Now create a feature1 branch from develop branch.
    In this feature1 branch performed multiple commits.
    pushed the branch to Github and created a pull request (PR) to develop.

feature2 Branch Creation:

    while feature1 PR was still under review.
    A new feature2 branch was created.
    add chnges and commited and pushed the branch.

feature1 barnch merge to develop branch:

    feature1 was merged into develop branch.

feature2 branch Rebase to Develop barnch:

    feature2 branch was rebased to develop branch and push code and created a PR.

Add tag:

    A version Tag created.(git tag v1.0.0)

feature3 & feature4 branch Creation:

    feature3 and feature4 these two branch created from develop.
    In feature3 branch readme file change and commit.
    and also feature3 branch also another commit add.

Cherry-pick commit:

    cherry-pick command use for feature3 commit was moved to feature4 branch.

Amend commit message:

    amend commit use for updated the commit message in feature4 branch.

Add commit & last commit delete:

    Added another 3 more commit add into feature4 branch and remove the last commit using (git reset HEAD~1).
